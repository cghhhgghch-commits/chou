export const app = null;
export const auth = {
  currentUser: null,
  signOut: async () => {},
};
export const db = null;
export const storage = null;

export const firebaseCompat = {
  enabled: false,
  message: 'Firebase is disabled. Supabase is the active backend for this app.',
};

